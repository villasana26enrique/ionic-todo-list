import { FiltroCompletadosPipe } from './filtro-completados.pipe';

describe('FiltroCompletadosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCompletadosPipe();
    expect(pipe).toBeTruthy();
  });
});
